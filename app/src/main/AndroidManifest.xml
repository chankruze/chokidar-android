<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.MANAGE_DEVICE_POLICY_CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"
        tools:ignore="HighSamplingRate" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- queries -->
    <queries>
        <intent>
            <action android:name="android.intent.action.BATTERY_CHANGED" />
        </intent>
    </queries>

    <!-- app -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhonePolice"
        tools:targetApi="31">
        <activity
            android:name=".ui.common.FullScreenImageActivity"
            android:exported="false" />
        <activity
            android:name=".ui.common.IntruderGalleryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.wrongpasswordalert.WrongPasswordAlertActivity"
            android:exported="false" />
        <activity
            android:name=".ui.chargerremovalalert.ChargerRemovalAlertActivity"
            android:exported="false" />
        <activity
            android:name=".ui.antitouchalert.AntiTouchAlertActivity"
            android:exported="false" />
        <activity
            android:name=".ui.pocketalarm.PocketAlarmActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.PinLoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.PinSetupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.fullbatteryalert.FullBatteryAlertActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.common.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- services -->
        <service
            android:name=".ui.fullbatteryalert.FullBatteryAlertService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
        <service
            android:name=".ui.antitouchalert.AntiTouchAlertService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
        <service
            android:name=".ui.pocketalarm.PocketAlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
        <service
            android:name=".ui.chargerremovalalert.ChargerRemovalAlertService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
        <service
            android:name=".ui.wrongpasswordalert.WrongPasswordAlertService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
        <service
            android:name=".services.CameraService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- receivers -->
        <receiver
            android:name=".ui.wrongpasswordalert.WrongPasswordReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />

            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
                <action android:name="android.app.action.ACTION_PASSWORD_FAILED" />
                <action android:name="android.app.action.ACTION_PASSWORD_SUCCEEDED" />
            </intent-filter>
        </receiver>

        <!-- providers -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
